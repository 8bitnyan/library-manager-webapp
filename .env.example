# Copy to `.env.development` / `.env.production` (gitignored) or use `.env*.local` overrides.
DATABASE_URL=sqlite.db
BETTER_AUTH_SECRET=
# Optional GitHub OAuth (leave unset to disable)
# BETTER_AUTH_GITHUB_CLIENT_ID=
# BETTER_AUTH_GITHUB_CLIENT_SECRET=
